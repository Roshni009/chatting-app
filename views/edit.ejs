<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your Chats</title>
    <style>

        body {
            margin-inline: auto;
            box-sizing: border-box;
            padding: 0;
            margin: 0;
            background: linear-gradient(90deg, #1a2345ee, #0E1024);
             margin-left: 30px;
             color: white;
             font-family: cursive;
        }

        h3 {
            color: pink;
        }

        p {
            font-size: 20px;
        }

        p > b {
            color: rgb(0, 119, 128);
        }

        .content {
            background: linear-gradient(90deg, #1a2345ee, #0E1024);
            color: white;
            
        }
       
        button {
            padding-inline: 35px;
            padding-block: 5px;
            background-color: aquamarine;
             margin-top: 20px;
             margin-left: 30px;
             border: none;
             color: crimson;
             font-weight: 700;
             border-radius: 10px;
             
        }
      
    </style>
</head>
<body>
   <h3 >Edit you chat: <%= chat._id %></h3> 
   <p>This chat was sent from <b><%= chat.from %></b> to <b><%= chat.to%></b></p>
   <form method="POST" action="/chats/<%= chat._id%>?_method=PUT">
    <textarea class="content"  name="msg" id="" cols="30" rows="10"><%= chat.msg %></textarea>
    <br />
    <button>Edit</button>
   </form>

</body>
</html>